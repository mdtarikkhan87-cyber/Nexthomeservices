# Implementation Notes — Tracked Open Items
| 2 | Admin journey-level design | `src/app/admin/**` (layout, overview, users, listings, complaints pages), `src/lib/admin.ts` (`isAdminEmail`), `src/lib/admin-mock-data.ts` (`useAdminUsers`/`useAdminListings`/`useAdminComplaints` + approve/reject/resolve actions), `auth-context.tsx` (`isAdmin` flag, `loginAsAdmin`), `login/page.tsx` | Admin identification is a hardcoded email list checked client-side only (`ADMIN_EMAILS` in `admin.ts`) — not real security, intended to move server-side once the backend exists and returns `isAdmin` on the authenticated user. Users/Listings/Complaints are functional against mock + `ListingsContext` data. Ads moderation is intentionally **not** included in this pass — deferred to whoever owns the advertiser flow |
These are **not resolved**. Each is a placeholder or a deliberately conservative default, not a product decision. Do not treat any of the code referenced below as final — resolve the underlying product question first (see PRODUCT_DECISIONS.md / SCREEN_BLUEPRINTS.md / USER_JOURNEYS.md Readiness Checks), then update the referenced code.

Items 10–12 were added by the 24 August 2026 client-revision pass — see [REVISION_LOG.md](./REVISION_LOG.md) for what that pass changed, which approved decisions it amends, and what still needs client sign-off.

| # | Item | Where it lives in code | Current placeholder behavior |
|---|---|---|---|
| 1 | Status color extension for rejected states | `src/app/globals.css` (`--color-status-rejected`) | A non-brand muted terracotta, chosen for legibility, pending sign-off from whoever owns the Brand Guidelines |
| 2 | Admin journey-level design | `src/lib/admin.ts`, `src/lib/auth-context.tsx` (`isAdmin`, `loginAsAdmin`), `src/components/admin/`, `src/app/admin/**`, `src/lib/admin-mock-data.ts` | PRD §6.8 still has no journey-level detail upstream — this is new scope defined during implementation, not a resolution of that gap. Admin is deliberately orthogonal to the RoleName/role-verification system, not a fifth role: `isAdminEmail()` checks one hardcoded, documented email against the login form and grants a synthetic `isAdmin: true` user via `loginAsAdmin()`, routed straight to `/admin` — no real authentication or authorization exists, and this check must move server-side once a backend does. `/admin/{users,listings,complaints}` read/mutate the existing `mock-data.ts` catalog and session-submitted listings (`lib/listings-context.tsx`) through `admin-mock-data.ts`, so approving a listing there is the same data the public site reads — except Complaints, which has no real submission pipeline yet (the public form doesn't persist what it collects) and is backed by its own small static seed until one exists. |
| 3 | Complaints form access model | `src/app/(public)/complaints/page.tsx` | Left open/unauthenticated as a conservative default; explicitly commented in the file as unresolved |
| 4 | Exact messaging verification gate | `src/components/property/ListingActions.tsx`, `ServiceContactAction.tsx`, `src/app/dashboard/messages/page.tsx` | Messaging is currently gated on authentication only (`useAuthGate`), not on role-verified state — intentional, since gating it further would be inventing the resolution |
| 5 | Review timeframe + rejected-item resubmission mechanism | `src/components/ui/StatusBanner.tsx` (pending copy), listing/service/ad detail pages | No duration stated; rejected items show status but no defined resubmit flow beyond a generic "Edit" |
| 6 | Effect of a lapsed subscription on a live listing | Not modeled | `subscriptionState` only gates *new* publication in `src/app/dashboard/listings/new/page.tsx`; nothing revokes a listing that's already live |
| 7 | Advertiser pricing/payment sequencing | `src/app/dashboard/ads/new/page.tsx`, `src/app/dashboard/ads/page.tsx` | Implemented as submit → "pending content review and terms" → (future) payment; upfront self-serve pricing was NOT assumed |
| 8 | Whether editing a live listing re-triggers review | `src/app/dashboard/listings/[id]/page.tsx` | "Edit Listing" button exists but has no wired behavior — deliberately inert rather than guessing |
| 9 | Final login credential mechanism | `src/app/login/page.tsx`, `src/components/shared/AuthGate.tsx` (inline login form) | Email/phone + password fields used as a labeled placeholder; no real credential validation exists (no backend). The demo sign-in is now a picker of three fixed accounts (`src/lib/demo-accounts.ts`) — Renter only, Landlord only, Landlord + Renter — because the conditional role prompt cannot be demonstrated from a single hard-coded user. The credential form itself validates nothing and signs in as the two-role account. Still a demo affordance, not authentication; it goes when a backend arrives. |
| 10 | Detail-page gating is enforced client-side only | `src/components/property/ListingDetailGate.tsx` | The server reduces a listing to its public `ListingTeaser` before it crosses the client boundary, so gated fields are not in the HTML or RSC payload — but the mock catalog is still bundled into the client JS that every visitor downloads (`lib/mock-data`, already imported by the public listings browser). Real enforcement means not returning gated fields to an unauthenticated request, which needs a backend. The component boundary is the correct shape for that swap. |
| 11 | Session/auth state does not survive a page reload | `src/lib/auth-context.tsx` | Auth is in-memory; only the *active role preference* persists, now in `localStorage["activeRole:" + user.id]` rather than sessionStorage, keyed per account and surviving logout by design (PRODUCT_DECISIONS.md "Role Selection & Active-Role Model" §2). A hard refresh still signs the user out. Deliberate — persisting a fake session would be inventing authentication (see #9). |
| 12 | Error/rejected colour vs. the "no invented shades" rule | `src/app/globals.css` (`--color-status-rejected`) | Website Revision Spec §3D requires every colour to match a brand hex exactly. The Brand Guidelines define no error colour, and reusing a brand blue would make "rejected" and "verified" indistinguishable. Escalates #1 rather than replacing it — still needs a brand-owner decision. |

None of these were silently resolved — not during the original audit pass, and not during the client-revision pass that followed. Where a fix touched adjacent code (e.g. gating listing/service *submission* on role-verified state, which the docs *do* already specify), that was implemented; every item above was left exactly as open as it was found.
