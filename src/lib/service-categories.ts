// Shared between the provider's create form (dashboard/service-listing) and
// the public directory's category filter (ServiceDirectory), so a category a
// provider can submit is always one a customer can filter by.
export const SERVICE_CATEGORIES = ["Electrician", "Plumber", "Mechanic", "Carpenter", "Painter"];
